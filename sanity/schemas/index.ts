import project from './project'
import team from './team'
import news from './news'
import { 
  quoteBlock, 
  pullQuoteBlock, 
  imageGalleryBlock, 
  calloutBlock, 
  inlineImageBlock,
  videoBlock 
} from './blocks'

export const schemaTypes = [
  project, 
  team, 
  news,
  quoteBlock,
  pullQuoteBlock,
  imageGalleryBlock,
  calloutBlock,
  inlineImageBlock,
  videoBlock,
]
